services:
  annotator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./user_dict:/app/user_dict
    environment:
      - USER_DICT_DIR=/app/user_dict
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
