services:
  annotator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./user_dict:/app/user_dict
    environment:
      - USER_DICT_DIR=/app/user_dict
      - APP_PORT=8000
      - UVICORN_WORKERS=1
    restart: unless-stopped
